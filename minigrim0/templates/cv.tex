\documentclass[11pt,a4paper,sans]{moderncv}

% moderncv themes
\moderncvstyle[right]{classic}
\moderncvcolor{grey}  % color options 'black', 'blue' (default), 'burgundy', 'green', 'grey', 'orange', 'purple' and 'red'
% character encoding
\usepackage[utf8]{inputenc}

% adjust the page margins
\usepackage[scale=0.79, top=0.4in, bottom=0.8in]{geometry}

\usepackage{url}

% personal data
{%- if profile %}
\name{ {{ profile.name.split(' ')[0] }} }{ {{ profile.name.split(' ')[1:] | join(' ') }} }
\title{ {{ profile.title | latex_escape }} }
\address{ {{ profile.address | latex_escape }} }
\email{ {{ profile.email }} }
\social[github]{ {{ profile.github_username }} }
\extrainfo{ {{ profile.birth_date }} }
{%- if profile.profile_image %}
\photo[64pt][0.4pt]{ {{ profile.profile_image.path }} }
{%- endif %}
{%- else %}
\name{Florent}{Grimau}
\title{Master Student in Embedded Systems}
\address{Stockholm - Sweden}
\email{grimauflorent@gmail.com}
\social[github]{Minigrim0}
\extrainfo{22 Sept 2000}
{%- endif %}

%----------------------------------------------------------------------------------
%            content
%----------------------------------------------------------------------------------
\begin{document}
%-----       resume       ---------------------------------------------------------
\makecvtitle{}

\section{Education}
{%- for education in edu %}
\cventry{ {{ education.start_date }}--{{ education.end_date }} }{ {{ education.name | latex_escape }} }{ {{ education.place | latex_escape }} }{}{}{}
{%- endfor %}

\section{Experience}
{%- for experience in exp %}
\cventry{ {{ experience.start_date }}--{{ experience.end_date }} }{ {{ experience.name | latex_escape }} }{ {{ experience.place | latex_escape }} }{}{}{ {%- if experience.description %}{{ experience.description | latex_escape }}{%- endif %}{%- if experience.link %}\\ \url{ {{ experience.link }} }{%- endif %} }
{%- endfor %}

\section{Competition and extra-scolar activities}
{%- for competition in com %}
\cvitemwithcomment{ {{ competition.date }} }{ {{ competition.name | latex_escape }} }{ {%- if competition.description %}{{ competition.description | latex_escape }}{%- endif %} }
{%- endfor %}

\section{Languages}
{%- for language in lan %}
\cvitem{ {{ language.name | latex_escape }} }{ {{ language.get_level_display }} }
{%- endfor %}

\section{Skills}
{%- for skill in ski %}
\cvitem{ {{ skill.name | latex_escape }} }{ {%- for i in range(skill.level) %}\faCircle{%- endfor -%}{%- for i in range(5 - skill.level) %}\faCircleO{%- endfor -%}{%- if skill.details %}, {{ skill.details | latex_escape }}{%- endif %} }
{%- endfor %}

\section{Interest}
{%- for interest in int %}
\cvitem{ {{ interest.category.name | latex_escape }} }{ {{ interest.name | latex_escape }} }
{%- endfor %}

\end{document}