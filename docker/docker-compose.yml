services:
  web:
    image: minigrim0-web:latest
    build:
      dockerfile: docker/web.dockerfile
      context: ..
    environment:
      - DEBUG=False
      - LOGLEVEL=INFO
      - REDIS_URL=redis://redis:6379
    secrets:
      - SECRET_KEY
      - DB_USER
      - DB_PASSWORD
      - DB_HOST
      - DB_NAME
      - ALLOWED_HOSTS
      - PLAUSIBLE_URL
      - PLAUSIBLE_DOMAIN
    volumes:
      - static_volume:/usr/src/app/static
    depends_on:
      - redis
      - postgres
    labels:
      ofelia.enabled: "true"
      ofelia.job-exec.githubcron.schedule: "@every 1d"
      ofelia.job-exec.githubcron.command: "/bin/minigrim0 >> minigrim0.log && python manage.py fetchrepositories >> repo.log"
      ofelia.job-exec.githubcron.timezone: "Europe/Stockholm"
    deploy:
      restart_policy:
        condition: on-failure
    networks:
      - private

  nginx:
    image: minigrim0-nginx:latest
    build:
      context: ./nginx
      dockerfile: Dockerfile
    volumes:
      - static_volume:/srv/static
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.minigrim0-web-insecure.entrypoints=http"
      - "traefik.http.routers.minigrim0-web-insecure.rule=Host(`minigrim0.xyz`)"
      - "traefik.http.middlewares.minigrim0-web-redirect.redirectscheme.scheme=https"
      - "traefik.http.routers.minigrim0-web-insecure.middlewares=minigrim0-web-redirect"
      - "traefik.http.routers.minigrim0-web.entrypoints=https"
      - "traefik.http.routers.minigrim0-web.rule=Host(`minigrim0.xyz`)"
      - "traefik.http.routers.minigrim0-web.tls=true"
      - "traefik.http.routers.minigrim0-web.tls.certresolver=http"
      - "traefik.http.routers.minigrim0-web.service=nginx-minigrim0-web"
      - "traefik.docker.network=traefik-proxy"
    depends_on:
      - web
    deploy:
      restart_policy:
        condition: on-failure
    networks:
      - traefik-proxy
      - private

  postgres:
    image: postgres:15
    secrets:
      - DB_USER
      - DB_NAME
      - DB_PASSWORD
    environment:
      - POSTGRES_USER_FILE=/run/secrets/DB_USER
      - POSTGRES_DB_FILE=/run/secrets/DB_NAME
      - POSTGRES_PASSWORD_FILE=/run/secrets/DB_PASSWORD
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    deploy:
      restart_policy:
        condition: on-failure
    networks:
      - private

  redis:
    image: redis:alpine
    volumes:
      - redis_data:/data
    deploy:
      restart_policy:
        condition: on-failure
    networks:
      - private

  ofelia:
    image: mcuadros/ofelia:latest
    command: daemon --docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    deploy:
      restart_policy:
        condition: on-failure
    depends_on:
      - web
    networks:
      - private

  plausible_db:
    image: postgres:15
    volumes:
      - plausible_db_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=plausible
      - POSTGRES_PASSWORD=plausible
      - POSTGRES_DB=plausible_db
    deploy:
      restart_policy:
        condition: on-failure
    networks:
      - private

  plausible_events_db:
    image: clickhouse/clickhouse-server:24.3.3.102-alpine
    volumes:
      - plausible_events_data:/var/lib/clickhouse
      - ./plausible/clickhouse-config.xml:/etc/clickhouse-server/config.d/logging.xml:ro
      - ./plausible/clickhouse-user-config.xml:/etc/clickhouse-server/users.d/logging.xml:ro
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    deploy:
      restart_policy:
        condition: on-failure
    networks:
      - private

  plausible:
    image: ghcr.io/plausible/community-edition:v2.1.1
    command: sh -c "sleep 10 && /entrypoint.sh db createdb && /entrypoint.sh db migrate && /entrypoint.sh run"
    depends_on:
      - plausible_db
      - plausible_events_db
    secrets:
      - PLAUSIBLE_SECRET_KEY_BASE
      - PLAUSIBLE_TOTP_VAULT_KEY
    environment:
      - BASE_URL=https://analytics.minigrim0.xyz
      - SECRET_KEY_BASE_FILE=/run/secrets/PLAUSIBLE_SECRET_KEY_BASE
      - TOTP_VAULT_KEY_FILE=/run/secrets/PLAUSIBLE_TOTP_VAULT_KEY
      - DATABASE_URL=postgres://plausible:plausible@plausible_db:5432/plausible_db
      - CLICKHOUSE_DATABASE_URL=http://plausible_events_db:8123/plausible_events_db
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.plausible-insecure.entrypoints=http"
      - "traefik.http.routers.plausible-insecure.rule=Host(`analytics.minigrim0.xyz`)"
      - "traefik.http.middlewares.plausible-redirect.redirectscheme.scheme=https"
      - "traefik.http.routers.plausible-insecure.middlewares=plausible-redirect"
      - "traefik.http.routers.plausible.entrypoints=https"
      - "traefik.http.routers.plausible.rule=Host(`analytics.minigrim0.xyz`)"
      - "traefik.http.routers.plausible.tls=true"
      - "traefik.http.routers.plausible.tls.certresolver=http"
      - "traefik.http.routers.plausible.service=plausible-service"
      - "traefik.http.services.plausible-service.loadbalancer.server.port=8000"
      - "traefik.docker.network=traefik-proxy"
    deploy:
      restart_policy:
        condition: on-failure
    networks:
      - traefik-proxy
      - private

volumes:
  static_volume:
  postgres_data:
  redis_data:
  plausible_db_data:
  plausible_events_data:

networks:
  traefik-proxy:
    external: true
  private:
    driver: overlay
    attachable: true

secrets:
  SECRET_KEY:
    external: true
  DB_USER:
    external: true
  DB_PASSWORD:
    external: true
  DB_HOST:
    external: true
  DB_NAME:
    external: true
  ALLOWED_HOSTS:
    external: true
  PLAUSIBLE_URL:
    external: true
  PLAUSIBLE_DOMAIN:
    external: true
  PLAUSIBLE_SECRET_KEY_BASE:
    external: true
  PLAUSIBLE_TOTP_VAULT_KEY:
    external: true
